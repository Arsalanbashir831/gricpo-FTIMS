import { SupervisorProfileScreen } from "@/features/auth/components/supervisor-profile-screen";

export const metadata = {
  title: "Supervisor Profile | GRIPCO FTIMS",
  description: "View account details, permissions, and session options.",
};

export default function SupervisorProfilePage() {
  return <SupervisorProfileScreen />;
}
